apiVersion: v1
kind: Pod
metadata:
  name: redpod
  namespace: production
  labels:
   app: web
spec:
  containers:
  - name: moncontainer2
    image: mmumshad/simple-webapp-color
    ports:
     - containerPort: 8080
    env:
     - name: APP_COLOR
       value: red

